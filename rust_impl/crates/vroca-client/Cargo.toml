[package]
name = "vroca-client"
version = "0.1.0"
edition = "2021"

[dependencies]
vroca-core = { path = "../vroca-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
[dev-dependencies]
tempfile = "3.10"
